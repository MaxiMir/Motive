export default {
  en: {
    getTitle(name: string): string {
      return `Support ${name}`
    },
  },
  ru: {
    getTitle(name: string): string {
      return `Поддержать ${name}`
    },
  },
  uk: {
    getTitle(name: string): string {
      return `Підтримати ${name}`
    },
  },
}

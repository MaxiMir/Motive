export default {
  en: {
    getTitle(name: string): string {
      return `Support ${name}`
    },
  },
  ru: {
    getTitle(name: string): string {
      return `Поддержка ${name}`
    },
  },
  uk: {
    getTitle(name: string): string {
      return `Підтримка ${name}`
    },
  },
}

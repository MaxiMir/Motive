export const enum Route {
  Feed = '/feed',
  Search = '/search',
  Rating = '/rating',
  Following = '/following',
  Contact = '/contact',
  Articles = '',
}

export const enum SearchParam {
  Dates = 'd',
  ScrollTo = 's',
  ScrollId = 'i',
}

export const enum HashMark {
  Goal = 'goal',
  Feedback = 'feedback',
  Discussion = 'discussion',
}
